import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const SignInScreen = () => {
  return <Text style={styles.text}>SignInScreen Screen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 20,
  },
});

export default SignInScreen;
